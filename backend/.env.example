# Django Configuration
# IMPORTANT: Do NOT commit your real SECRET_KEY to source control.
# Set the production SECRET_KEY in your Railway (or hosting) environment variables.
# Use the generated key from the command or create a secure one.
# Example (Railway):
#   SECRET_KEY=p8mh##l1kmv$6f8pb!ao$ld0^rai_1pwnt7$9u4_fc(++tpr3#
SECRET_KEY=django-insecure-your-secret-key-change-in-production

# Environment (development|production)
# In production set ENVIRONMENT=production and DEBUG=False
ENVIRONMENT=development
DEBUG=True

# Allowed Hosts (comma-separated, no spaces)
# Development: localhost,127.0.0.1
# Production example (Railway backend): edureach-production.up.railway.app,*.up.railway.app
ALLOWED_HOSTS=localhost,127.0.0.1

# Google Gemini API (Get key from: https://makersuite.google.com/app/apikey)
GEMINI_API_KEY=your-gemini-api-key-here

# CORS Settings (comma-separated, no spaces)
# Add your frontend URLs here. In production include the frontend origin deployed on Railway.
# Development: http://localhost:5173,http://localhost:3000
# Production (Railway frontend): https://melodious-cooperation-production.up.railway.app
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Database (optional, defaults to SQLite)
# For PostgreSQL on Railway, set the DATABASE_URL environment variable provided by Railway.
# Example (Railway will provide a URL):
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Redis (optional, for caching)
# REDIS_URL=redis://localhost:6379/1
